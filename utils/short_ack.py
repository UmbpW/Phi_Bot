"""Определение коротких подтверждений «давай/ок/угу» для pending follow-through."""

SHORT_ACK_PHRASES = (
    "да",
    "давай",
    "ок",
    "окей",
    "угу",
    "хорошо",
    "+",
    "👍",
    "ага",
    "давай так",
    "поехали",
    "го",
)


def is_short_ack(text: str) -> bool:
    """True если текст — короткое подтверждение (да, давай, ок, угу и т.п.)."""
    if not text:
        return False
    t = (text or "").strip().lower().replace(" ", "")
    if not t or len(t) > 20:
        return False
    return t in SHORT_ACK_PHRASES or t in ("👍", "👌", "✓", "✔", "➕")
